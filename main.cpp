#include <iostream>

constexpr int N_ELEMENTS = 100;

int main()
{
    int *b = new int[NELEMENTS];                            // Helytelen hivatkozás és kibővítjük a tömböt
    std::cout << '1-100 ertekek duplazasa' for (int i = 0;) // Hiányzó pontosvessző és kiegészítjök a ciklus követelményeit
    {
        b[i] = i * 2;
    }
    for (int i = 0; i; i++) // Kiegészítjök a ciklus követelményeit
    {
        std::cout << "Ertek:" // Hiányzó pontosvessző és az értékek kimutatása
    }
    std::cout << "Atlag szamitasa: " << std::endl;
    int atlag;                           // Inancializáció
    for (int i = 0; i < N_ELEMENTS, i++) // Hiányzó pontosvessző
    {
        atlag += b[i] // Hiányzó pontosvessző
    }
    atlag /= N_ELEMENTS;
    std::cout << "Atlag: " << atlag << std::endl;
    return 0;
}